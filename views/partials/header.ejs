<!DOCTYPE html>
<html>
  <head>
	<meta charset="utf-8">
    <title>
    <% if(!title) { %>
    Sails JS
    <% } else {%>
    <%= title %>  
    <% } %>
    </title>
    <!-- Viewport mobile tag for sensible mobile support -->
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Muli">
    <!--STYLES-->
    <link rel="stylesheet" href="/styles/bootstrap/bootstrap-theme.css">
    <link rel="stylesheet" href="/styles/bootstrap/bootstrap.css">
    <link rel="stylesheet" href="/styles/importer.css">
    <link rel="stylesheet" href="/styles/styles.css">
    <!--STYLES END-->
  </head>

  <body class="body">
	  <header class="col-md-12 header">
      <div class="row">
        <div class="col-md-2 header-info">
          <label class="col-md-12 header-title">Sails</label>
          
          <label class="col-md-12 header-descrip">By NodeJS</label>
        </div>
        <div class="col-md-10 header-items">
      		<nav class="header-nav">
      		  <ul class="header-nav-ul">
              <li class="ul-li"><a href="/" class="link1">Home</a></li>
              <% if(!req.user){ %>
              <li class="ul-li"><a href="/user/create" class="link1">Signup now</a></li>
              <li class="ul-li"><a href="/login" class="link1">Login</a></li>
              <% } else { %>
              <li class="ul-li"><a href="/user" class="link1">Usuarios</a></li>
              <li class="ul-li"><a href="/user/<%= req.user.id  %>" class="link1"><%= req.user.email %></a></li>
              <li class="ul-li"><a href="/logout" class="link1">Logout</a></li>
              <% } %>
            </ul>
      		</nav>
        </div>
      </div>
	  </header>